# Proyecto Node de ejemplo

Pequeño proyecto Node.js creado automáticamente.

Comandos:

- Iniciar: `npm start` (ejecuta `node src/index.js`)
- Test: `npm test` (ejecuta `node test/test.js`)

No hay dependencias externas.
